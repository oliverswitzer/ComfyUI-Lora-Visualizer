# Requirements for LoRA Visualizer and Prompt Splitter Custom Nodes
# The LoRA visualizer depends only on ComfyUI built‑ins, but the prompt
# splitter requires the `requests` library to contact your local Ollama
# server.  Install these packages into the same environment as ComfyUI.

requests>=2.28.0

# Development dependencies (optional)
pytest>=7.0.0
black>=22.0.0
pylint>=2.12.0